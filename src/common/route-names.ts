export enum RouteNames {
  // Slug routes
  METRICS = 'metrics',
  HEALTH = 'health',
  AUTH = 'auth',
  GRAPHQL = 'graphql',
  API_DOCS = 'api',

  // Named routes
  INCREMENT = 'increment',
  ACTIVE_USERS = 'active-users',
  LOGIN = 'login',
  LOGOUT = 'logout',
  REGISTER = 'register',
}
