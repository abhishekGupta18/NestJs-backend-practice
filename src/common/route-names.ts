export enum RouteNames {
  // Slug routes
  METRICS = 'metrics',
  HEALTH = 'health',
  AUTH = 'auth',

  // Named routes
  INCREMENT = 'increment',
  ACTIVE_USERS = 'active-users',
  LOGIN = 'login',
  LOGOUT = 'logout',
  REGISTER = 'register',
}
